# ============================================================================
# Compre Aqui - Environment Configuration Template
# Copy this file to .env.dev or .env.prod and fill in the values.
# NEVER commit .env files with real credentials to version control.
# ============================================================================

# App Configuration
APP_NAME=Compre Aqui
API_BASE_URL=https://southamerica-east1-YOUR_PROJECT_ID.cloudfunctions.net/api
LOG_LEVEL=error
ENABLE_LOGGING=false

# Payment Gateway - Mercado Pago
# Get these from: https://www.mercadopago.com.br/developers/panel/app
# IMPORTANT: Use TEST credentials for development, PRODUCTION credentials for prod
MP_PUBLIC_KEY=APP_USR-xxxx
MP_ACCESS_TOKEN=APP_USR-xxxx
MP_CLIENT_ID=xxxx
MP_CLIENT_SECRET=xxxx

# Webhook Secret - REQUIRED for production
# Generate at: Mercado Pago Dashboard > Integraciones > Webhooks
# This is critical for validating incoming webhook notifications
MP_WEBHOOK_SECRET=xxxx

# OAuth Redirect URI for seller connection
# Must match the URL registered in Mercado Pago application settings
MP_OAUTH_REDIRECT_URI=https://southamerica-east1-YOUR_PROJECT_ID.cloudfunctions.net/api/mp-oauth-callback

# Split Payment Configuration
PLATFORM_FEE_PERCENTAGE=10.0
PAYMENT_HOLD_HOURS=24

# ============================================================================
# Firebase Cloud Functions - Production Deployment
# ============================================================================
# For production, use Firebase Secret Manager instead of .env files:
#
#   firebase functions:secrets:set MP_ACCESS_TOKEN
#   firebase functions:secrets:set MP_CLIENT_SECRET
#   firebase functions:secrets:set MP_WEBHOOK_SECRET
#
# Then reference them in your function config or code:
#   defineSecret("MP_ACCESS_TOKEN")
#
# See: https://firebase.google.com/docs/functions/config-env#secret-manager
# ============================================================================
